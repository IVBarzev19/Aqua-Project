import React from "react";
import Panel from "./Panel";

const PanelGrid = () => {
  return (
    <div className="panel-grid">
      <Panel />
      <Panel />
      <Panel />
      <Panel />
    </div>
  );
};

export default PanelGrid;
